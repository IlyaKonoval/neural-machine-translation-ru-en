[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "transformer-ru-en-translator"
version = "1.0.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["src", "src.*", "api", "api.*"]
